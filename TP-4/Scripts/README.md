# Instrucciones para ejecutar el 
### Es necesario tener instalada la bilbioteca GTSAM
### En caso de utilizar un ambiente aisaldo CONDA o un ambiente virtual de python con el wrapper de GTSAM es necesario tener correctaemnte seleccionado el kernel del notebook para que el código se pueda ejecutar, prestar especial atención al utilizar VScode.
### Para correr el TP correctamente se recomienda ejecutarlo en el orden en que estan las celdas (mismo orden de los ejercicios del TP).
### Si no se utiliza un nootebok se pueden tener problemas con la generación de imagenes, en ese caso se recomienda des-comentar la linea < matplotlib.use("Agg")   # seguro para WSL/servidores; cambiar si necesitás mostrar inline > y visualizar las imagenes desde los archivos guardados en formato .png



